import React from 'react';
import styled from 'styled-components';

const Info = () => {
    return (
        <InfoContainer>
            
        </InfoContainer>
    );
};

export default Info;

const InfoContainer = styled.div`
    width: 43%;
`;